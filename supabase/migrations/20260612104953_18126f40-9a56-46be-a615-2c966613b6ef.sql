ALTER TABLE public.user_connections ADD COLUMN IF NOT EXISTS last_seen_at timestamptz NOT NULL DEFAULT now();
CREATE INDEX IF NOT EXISTS user_connections_user_last_seen_idx ON public.user_connections (user_id, last_seen_at DESC);